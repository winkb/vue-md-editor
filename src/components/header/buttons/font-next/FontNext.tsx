import { defineComponent } from "vue";

const FontNextComponent = defineComponent({
    setup() {
        return () => <button class="button iconfont icon-cancel"></button>
    }
});

export default FontNextComponent