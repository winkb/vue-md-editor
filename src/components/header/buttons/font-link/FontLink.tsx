import { defineComponent } from "vue";

const FontLinkComponent = defineComponent({

    setup() {
        return () => (
            <button class=" button iconfont icon-lianjie"></button>
        )
    }
});

export default FontLinkComponent