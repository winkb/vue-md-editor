import { defineComponent } from "vue";

const FontTableComponent = defineComponent({

    setup() {
        return () => (
            <button class=" button iconfont icon-biaoge"></button>
        )
    }
});

export default FontTableComponent